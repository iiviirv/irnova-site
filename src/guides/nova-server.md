# راهنمای کامل نوا سرور (⁦Nova Server⁩)

⁦Nova Server⁩ یک ⁦VPS⁩ ساده لینوکسی را به یک نود پروکسی خصوصی و مقاوم در برابر فیلترینگ تبدیل می‌کند، همراه با یک پنل مدیریت کامل. جایی که ⁦Nova Proxy⁩ روی لایه رایگان ⁦Cloudflare⁩ اجرا می‌شود، ⁦Nova Server⁩ نسخه خودمیزبان و قدرتمندتر است: یک هسته پروکسی واقعی با هر آنچه یک اپراتور جدی نیاز دارد. زیر کاپوت، ⁦Xray-core⁩، ⁦sing-box⁩ (برای ⁦Hysteria2⁩) و ⁦AmneziaWG⁩ را پشت یک پورت اجرا می‌کند و همه را با یک ایجنت خودمیزبان مدیریت می‌کند.

این راهنما شما را قدم‌به‌قدم از تهیه ⁦VPS⁩ تا مدیریت روزانه می‌برد. در انتها یک بخش «سوالات متداول» آمده است. اگر تا حالا سرور راه نینداخته‌اید هم نگران نباشید؛ هر کلمه فنی همان اولین بار توضیح داده می‌شود.

---

## پیش‌نیازها

- یک ⁦VPS⁩ با **⁦Ubuntu 20.04⁩ به بالا** یا **⁦Debian 11⁩ به بالا** (معماری ⁦x86_64⁩ یا ⁦arm64⁩). ⁦VPS⁩ یک کامپیوتر کوچک است که روی اینترنت اجاره می‌کنید و همیشه روشن است، تا کاربران بتوانند شبانه‌روز به آن وصل شوند. هر ارائه‌دهنده‌ای کار می‌کند؛ یک سرور ساده (حدود ۴ تا ۶ دلار در ماه) کافی است.
- دسترسی **⁦root⁩** به سرور (بالاترین سطح دسترسی که اجازه نصب می‌دهد).
- (اختیاری) یک **دامنه**. فقط برای گرفتن گواهی معتبر و ⁦Mini App⁩ تلگرام لازم می‌شود. برای شروع به دامنه **نیاز ندارید**؛ نود روی ⁦IP⁩ خودش کار می‌کند و اپ ⁦Nova⁩ باز هم وصل می‌شود.

---

## گام ۱ - نصب نود

روی یک سرور تازه ⁦Ubuntu 20.04⁩ به بالا یا ⁦Debian 11⁩ به بالا، فقط همین یک دستور را اجرا کنید:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/IRNova/Nova-Server/main/nova-node.sh)
```

اگر تا حالا از ⁦SSH⁩ استفاده نکرده‌اید (که فقط راهی برای تایپ‌کردن دستور روی سرور است)، بیشتر ارائه‌دهنده‌ها یک دکمه ⁦Console⁩ در مرورگر دارند؛ همان را باز کنید، دستور بالا را بچسبانید و ⁦Enter⁩ بزنید.

نصب‌کننده اول چند سوال کوتاه می‌پرسد و زدن ⁦Enter⁩ برای پذیرفتن پیش‌فرض‌ها کافی است:

- **دامنه:** اگر دامنه دارید، به‌صورت خودکار یک گواهی رایگان ⁦Let's Encrypt⁩ برایش گرفته می‌شود. اگر دامنه ندارید هم مشکلی نیست: نود روی ⁦IP⁩ خودش با یک گواهی خودامضا (⁦self-signed⁩) کار می‌کند.
- **مسیر مخفی پنل:** با زدن ⁦Enter⁩ یک مسیر تصادفی ساخته می‌شود، می‌توانید مسیر دلخواه خودتان را بنویسید، یا با نوشتن ⁦none⁩ پنل را روی ریشه نگه دارید.
- **پورت اضافی پنل:** در صورت تمایل، یک پورت ⁦HTTPS⁩ جداگانه برای پنل (مثلا ⁦2053⁩). پورت در فایروال به‌صورت خودکار باز می‌شود.

سپس هسته‌های پروکسی، پنل و بک‌اندهای تونل راه‌اندازی می‌شوند و آدرس پنل (همراه با مسیر مخفی) نمایش داده می‌شود. **حتما آن آدرس را ذخیره کنید.**

برای نصب اسکریپتی و بدون سوال، همه‌چیز را با متغیرهای محیطی تنظیم کنید: ⁦NOVA_DOMAIN⁩، ⁦NOVA_DOMAIN_EMAIL⁩، ⁦NOVA_PANEL_PATH⁩ (یا ⁦none⁩)، ⁦NOVA_PANEL_PORT⁩، ⁦NOVA_ADMIN_PASS⁩ و برای رد شدن از همه سوال‌ها ⁦NOVA_NO_PROMPT=1⁩.

**بدون رایانه؟** می‌توانید کل نود را از گوشی راه بیندازید. هنگام ساخت ⁦VPS⁩ در اپ یا سایت ارائه‌دهنده، همین دستور را در کادر ⁦User data⁩ یا ⁦Cloud-init⁩ بچسبانید. سرور خودش هنگام اولین بوت ⁦Nova⁩ را نصب می‌کند (حدود ۳ تا ۵ دقیقه)، بعد آدرس ⁦https://YOUR_SERVER_IP⁩ را در مرورگر گوشی باز کنید و رمز بگذارید. راه ساده‌تر: از **ربات نصب‌کننده تلگرام** استفاده کنید (گام ۱۳ را ببینید).

---

## گام ۲ - اولین ورود و تعیین رمز ادمین

نصب‌کننده آدرس پنل شما را چاپ می‌کند، شامل یک مسیر مخفی تا اسکنرها نتوانند پیدایش کنند، پس آن لینک را ذخیره کنید. آن را باز کنید و یک رمز مدیریت (⁦admin⁩) بگذارید. رمز قوی انتخاب کنید و در جای امن نگه دارید.

آدرس یا مسیر مخفی پنل را گم کرده‌اید؟ روی سرور این دستور را اجرا کنید؛ همین دستور رمز را بازنشانی می‌کند و آدرس فعلی پنل را هم چاپ می‌کند:

```bash
nova-passwd 'YourNewPassword' --clear-2fa
```

---

## گام ۳ - نگاهی به پنل

بعد از ورود، داشبورد را می‌بینید: نمودارهای ترافیک، وضعیت سرویس و دسترسی سریع به همه بخش‌ها. پنل سه‌زبانه است (⁦English⁩، فارسی راست‌به‌چپ و ⁦Русский⁩) و تم روشن و تیره دارد.

بخش‌های اصلی که با آن‌ها سروکار دارید:

- **⁦Users⁩ (کاربران):** ساخت و مدیریت کاربرها، سهمیه حجم، انقضا و لینک اشتراک.
- **⁦Inbounds⁩ (ورودی‌ها):** ساخت پروتکل‌ها و نقاط ورود، همراه با نمودار ترافیک روزانه هر ورودی.
- **⁦Nodes⁩ (نودها):** مدیریت ناوگان چندنودی.
- **⁦Tunnels⁩ (تونل‌ها):** ساخت پل ایران.
- **⁦Settings⁩ (تنظیمات):** دامنه و ⁦SSL⁩، امنیت، دسترسی پنل، خودکارسازی و بقیه تنظیمات.

خود پنل یک راهنمای **راه‌اندازی گام‌به‌گام** مفصل هم به‌صورت داخلی دارد (بالای راهنمای داخل پنل)، به هر سه زبان. اگر جایی گیر کردید، اول همان را ببینید.

---

## گام ۴ - راه‌انداز خودکار (⁦Setup Wizard⁩)

بعد از تعیین رمز، ساده‌ترین راه شروع، **راه‌انداز خودکار** است. این ویزارد شما را در سه کار همراهی می‌کند: افزودن یک دامنه، ساخت یک پروتکل پیشنهادی و ساخت اولین کاربر. اگر تازه‌کارید، همین را دنبال کنید تا در چند دقیقه آنلاین شوید.

---

## گام ۵ - دامنه و ⁦SSL⁩ (اختیاری ولی توصیه‌شده)

دامنه (یک نشانی وب مثل ⁦vpn.example.com⁩) و گواهی ⁦SSL⁩ (همان قفلی که اتصال را مطمئن می‌کند) اختیاری‌اند. بدون آن‌ها، نود شما روی ⁦IP⁩ کار می‌کند و اپ ⁦Nova⁩ باز هم وصل می‌شود. با یک دامنه، پنل به‌صورت خودکار یک گواهی رایگان می‌گیرد.

⁦Nova Server⁩ سه راه برای گواهی دارد و هر سه خودکار تمدید می‌شوند:

- **⁦Let's Encrypt⁩ یک‌کلیکی:** ساده‌ترین راه، بدون درگیری دستی با پورت ۸۰.
- **⁦Cloudflare⁩ کاملا خودکار:** ⁦DNS⁩ خودکار به‌همراه وایلدکارت.
- **گواهی ⁦Origin⁩:** اگر گواهی خودتان را دارید، می‌چسبانید.

می‌توانید این مرحله را رد کنید و بعدا از بخش تنظیمات اضافه کنید.

---

## گام ۶ - افزودن کاربر و گرفتن لینک اشتراک

**سریع‌ترین راه:** در نصب تازه، ⁦Nova⁩ همین حالا یک کاربر آغازین با نام ⁦me⁩ را با دسترسی به همهٔ پروتکل‌ها ساخته است. کافی است در **داشبورد اصلی** کارت «اتصال سریع» را پیدا کنید و کد ⁦QR⁩ آن را با اپ ⁦Nova⁩ یا ⁦Hiddify⁩ اسکن کنید، با یک اسکن آنلاین می‌شوید. لازم نیست چیزی را دستی کپی کنید.

وقتی کاربر بیشتری می‌خواهید، در بخش **⁦Users⁩** یک کاربر اضافه کنید و لینک اشتراک (⁦subscription⁩) او را کپی کنید. لینک اشتراک یک لینک است که همه تنظیمات اتصال را در خود دارد و خودش به‌روز می‌ماند. آن را در اپ **⁦Nova⁩** باز کنید، کد ⁦QR⁩ را با گوشی اسکن کنید، یا در یک کلاینت معمولی مثل ⁦v2rayNG⁩، ⁦sing-box⁩ یا ⁦Clash⁩ بچسبانید. آنلاین شدید.

⁦Nova Server⁩ کنترل کامل هر کاربر را می‌دهد:

- **سقف حجم:** کل، یا جدا برای دانلود و آپلود.
- **انقضا:** تاریخ ثابت، یا از اولین اتصال.
- **محدودیت دستگاه/⁦IP⁩:** تعداد دستگاه هم‌زمان.
- **بازنشانی حجم:** روزانه، هفتگی یا ماهانه.
- **دسترسی پروتکل و ورودی هر کاربر:** هر کاربر فقط به پروتکل‌های مجازش وصل می‌شود.
- **پلن‌های ذخیره‌شده:** یک مجموعه محدودیت قابل‌استفاده مجدد (مثلا ۳۰ روز / ۵۰ گیگ / ۳ دستگاه) که روی یک یا چند کاربر اعمال می‌شود.
- **عملیات گروهی:** فعال‌سازی، غیرفعال‌سازی، تمدید، بازنشانی، اعمال پلن یا حذف روی چند کاربر انتخاب‌شده با هم.
- **ورود/خروج ⁦CSV⁩:** فهرست کاربران را دسته‌ای وارد یا خارج کنید (به‌روزرسانی خودکار بر اساس نام).

لینک اشتراک هر کاربر یک صفحه مصرف زنده، کد ⁦QR⁩ و فرمت‌های ⁦Clash/Mihomo⁩ و ⁦sing-box⁩ هم دارد.

---

## گام ۷ - پروتکل‌ها و ورودی‌ها

پروتکل‌ها را در صفحه **⁦Inbounds⁩** می‌سازید. ⁦Nova Server⁩ همه پروتکل‌های مهم را روی یک صفحه مدیریت می‌کند:

- ⁦VLESS⁩، ⁦VMess⁩، ⁦Trojan⁩، ⁦Shadowsocks-2022⁩
- ⁦VLESS-Reality⁩ (با ⁦XTLS-Vision⁩)
- ⁦Hysteria2⁩، ⁦TUIC v5⁩، ⁦NaiveProxy⁩
- ⁦WireGuard⁩ بومی و ⁦AmneziaWG⁩

ترنسپورت‌های در دسترس: ⁦TCP⁩، ⁦WebSocket⁩، ⁦gRPC⁩، ⁦XHTTP⁩، ⁦HTTPUpgrade⁩ و ⁦mKCP⁩، روی ⁦TLS⁩ یا ⁦Reality⁩.

اگر تازه‌کارید و نمی‌دانید کدام را بسازید، از پیشنهاد **راه‌انداز خودکار** استفاده کنید. برای مقاومت بالا در برابر فیلترینگ، ⁦VLESS-Reality⁩ و ⁦Hysteria2⁩ گزینه‌های خوبی هستند. در صفحه ورودی‌ها یک نمودار ترافیک روزانه برای هر ورودی هم می‌بینید تا بفهمید کدام نقاط ورود بار بیشتری دارند.

**⁦Hysteria2⁩ مقاوم‌تر:** هنگام ساخت یک ورودی ⁦Hysteria2⁩ سه گزینهٔ اضافه دارید. «پرش پورت (بازهٔ ⁦UDP⁩)» را با یک بازه مثل ⁦40000-50000⁩ و یک فاصلهٔ پرش (مثلا ۳۰ ثانیه) تنظیم کنید تا اتصال روی یک پورت ثابت گیر نکند. «مبهم‌سازی» (⁦Salamander⁩) را با یک رمز مشترک روشن کنید تا ترافیک ⁦QUIC⁩ سخت‌تر انگشت‌نگاری شود. «⁦ECH⁩ (پنهان‌کردن ⁦SNI⁩)» را فعال کنید تا نام دامنه در دست‌دادن ⁦TLS⁩ دیده نشود؛ پنل کلید ⁦ECH⁩ را خودش روی سرور می‌سازد و فقط کانفیگ عمومی‌اش در اشتراک می‌رود، کلید خصوصی هیچ‌وقت به کلاینت نمی‌رسد. کانفیگ کامل پرش پورت و ⁦ECH⁩ فقط در فرمت ⁦sing-box⁩ تحویل داده می‌شود، پس اشتراک را در اپ ⁦sing-box⁩ یا ⁦Hiddify⁩ ایمپورت کنید.

**مسیریابی و خروج:** در تنظیمات می‌توانید قوانین نقطه‌ای ⁦geosite/geoip/CIDR⁩/دامنه/پروتکل بگذارید، دسترسی مستقیم ایران و بایپس داخلی را فعال کنید، تبلیغات/تورنت/⁦QUIC⁩ را مسدود کنید و ⁦DNS⁩ امن و ضدتحریم بگذارید. برای خروج ضدفیلترینگ، ⁦WARP⁩ (با لایسنس ⁦WARP+⁩ خودتان)، ⁦Tor⁩ و ⁦Psiphon⁩ به‌صورت داخلی موجودند و می‌توانید هر خروجی را به یک ورودی اختصاص دهید.

---

## گام ۸ - کانفیگ‌های هر اپراتور (اختیاری)

در تنظیمات، گزینه **کانفیگ‌های هر اپراتور در اشتراک** را روشن کنید تا اشتراک هر کاربر یک کانفیگ اضافه برای هر اپراتور ایرانی بگیرد (ایرانسل، همراه اول، رایتل، شاتل، موبین‌نت)، هرکدام با اثر انگشت ⁦TLS⁩ همان اپراتور تنظیم‌شده و با برچسب اپراتور. کاربران روی کلاینت‌های معمولی (⁦v2rayNG⁩، ⁦sing-box⁩، ⁦Clash⁩) فقط همان کانفیگی را که با سیم‌کارتشان می‌خواند انتخاب می‌کنند، بدون نیاز به اپ خاص. این گزینه به‌صورت پیش‌فرض خاموش است و روی فرمت‌های ساده، ⁦Clash⁩ و ⁦sing-box⁩ اعمال می‌شود.

---

## گام ۹ - ناوگان چندنودی (چند سرور از یک پنل)

دیگر لازم نیست روی هر سرور یک پنل کامل نصب کنید. پنل را یک بار روی سرور اصلی نصب کنید و ناوگان را از همان‌جا گسترش دهید:

۱. در پنل اصلی، صفحه **⁦Nodes⁩** را باز کنید و روی **افزودن نود با یک دستور** بزنید.

۲. تک‌خطی را که به شما می‌دهد روی یک ⁦VPS⁩ تازه اجرا کنید:

```bash
NOVA_JOIN_URL='https://your-panel' NOVA_JOIN_TOKEN='njt_...' bash <(curl -fsSL https://raw.githubusercontent.com/IRNova/Nova-Server/main/nova-node.sh)
```

سرور جدید در حالت **نود مدیریت‌شده** نصب می‌شود: پنل مستقلی ندارد (فقط یک صفحه ساده، بدون امکان ورود)، خودش را خودکار به پنل اصلی معرفی می‌کند و از آن به بعد همه‌چیز (کاربران، ترافیک، همه‌چیز) از داخل پنل اصلی کنترل می‌شود. توکن اتصال یک‌بارمصرف است و بعد از ۲۴ ساعت منقضی می‌شود. این کار چه نود دامنه داشته باشد و چه روی یک ⁦IP⁩ ساده با گواهی خودامضا اجرا شود کار می‌کند. اتصال روی ⁦TLS⁩ تاییدشده انجام می‌شود و توکن در مسیر قابل‌شنود نیست.

**اگر ثبت‌نام کامل نشود، چیزی از دست نمی‌رود.** معمولا علتش این است که پورت ۴۴۳ روی نود جدید هنوز به‌روی اینترنت باز نیست. در آن حالت نود در صفحه **⁦Nodes⁩** با وضعیت **در انتظار** و همراه دلیلش نمایش داده می‌شود. پورت ۴۴۳ را باز کنید و **⁦Test⁩** را بزنید، یا دستی اضافه‌اش کنید. نودی که در ثبت‌نام ناموفق بماند قفل نمی‌شود و به‌صورت یک پنل مستقل عادی باقی می‌ماند.

**کاربران و پروتکل‌ها مرکزی‌اند و روی نودها قرار می‌گیرند.** شما هیچ‌وقت مستقیم یک نود را مدیریت نمی‌کنید. کاربرها را در صفحه **⁦Users⁩** و پروتکل‌ها را در صفحه **⁦Inbounds⁩** مثل همیشه بسازید، و هنگام افزودن یک پروتکل با **⁦Run on⁩** انتخاب کنید کجا اجرا شود: این سرور اصلی، یک نود، یا **همه نودها**. کانفیگ اشتراک هر کاربر از **⁦IP⁩ همان نود** استفاده می‌کند. در صفحه **⁦Users⁩** انتخاب می‌کنید هر کاربر کدام نودها را بگیرد: زیر **نودهای این کاربر** نودهای مشخصی را تیک بزنید، یا **دسترسی به همه نودها** را روشن کنید.

**حذف یا بازیابی یک نود.** در صفحه **⁦Nodes⁩** روی **⁦Remove⁩** بزنید تا نود از ناوگان خارج شود. گزینه **حذف کامل نوا از آن سرور** را هم بزنید تا نود خودش را کامل پاک کند. برای تبدیل یک نود مدیریت‌شده به پنل مستقل عادی، روی آن سرور از طریق ⁦SSH⁩ دستور زیر را اجرا کنید:

```bash
nova-unlock 'YourNewPassword'
```

---

## گام ۱۰ - تونل‌های پل ایران

می‌خواهید یک ⁦IP⁩ تمیز داخل ایران جلوی خروج خارجی‌تان قرار بگیرد، بدون نصب کل استک روی سرور ایران؟ لازم نیست:

۱. در پنل نود **خارجی (خروج)**، بخش **⁦Tunnels⁩** را باز کنید، تونل را تنظیم کنید و روی **ساخت دستور پل ایران** بزنید.

۲. آن دستور تک‌خطی را روی **⁦VPS⁩ ایران** اجرا کنید. فقط بک‌اند تونل نصب می‌شود (⁦Backhaul⁩، ⁦BackPack⁩، ⁦rathole⁩ یا ⁦wstunnel⁩) و به‌صورت یک سرویس (⁦nova-tunnel⁩) بالا می‌آید. نه ⁦xray⁩، نه ⁦sing-box⁩، نه پنل.

کاربران شما به ⁦IP⁩ تمیز ایران وصل می‌شوند و ترافیک از آنجا به خروج خارجی تونل می‌شود. تونل هم ⁦TCP⁩ و هم ⁦UDP⁩ را منتقل می‌کند تا ⁦Hysteria2⁩ کار کند. این دستور راز مشترک تونل را در خود دارد، پس آن را خصوصی نگه دارید. **تونل چندگانه (پل‌های جایگزین):** می‌توانید چند پل ایران داشته باشید تا اگر یکی افتاد، اتصال قطع نشود. چند ⁦VPS⁩ ایران را با همان دستور پل بالا بیاورید (همه باید همان توکن، همان ترنسپورت و همان پورت‌های فوروارد را داشته باشند، یعنی آینهٔ هم باشند)، بعد در بخش **⁦Tunnels⁩** نود خروج، در فیلد «پل‌های جایگزین (اختیاری)» آدرس بقیهٔ پل‌ها را (هر کدام یک خط) بگذارید. خروج به همهٔ پل‌ها هم‌زمان وصل می‌شود و اشتراک هر کاربر هر لینک را یک‌بار به‌ازای هر پل می‌سازد، پس اگر ⁦IP⁩ یک پل مسدود شد، کلاینت خودکار سراغ پل زندهٔ بعدی می‌رود.

برای حذف پل بعدا این را اجرا کنید:

```bash
systemctl disable --now nova-tunnel
```

---

## گام ۱۱ - تونل ⁦DNS⁩ (آخرین راه)

⁦Nova⁩ یک تونل ⁦DNS⁩ داخلی و اختیاری دارد که ترافیک را داخل کوئری‌های ⁦DNS⁩ جابه‌جا می‌کند. چون روی ⁦DNS⁩ سوار می‌شود، وقتی همه‌چیز مسدود است ولی ⁦DNS⁩ هنوز جواب می‌دهد باز هم کار می‌کند؛ پس آخرین راه انتقال است، برای وقتی که هیچ روش دیگری رد نمی‌شود.

موتور تونل را در همان کارت انتخاب می‌کنید:

- **⁦MasterDnsVPN⁩** گزینه پیش‌فرض و ساده است.
- **⁦CottenDns⁩** موتور تواناتر است و برای شبکه‌های بسیار پرافت یا به‌شدت پایش‌شده ساخته شده: ترابری ⁦resolver⁩ از نوع ⁦DNS-over-TLS⁩ و ⁦DNS-over-HTTPS⁩، توزیع بار روی چند ⁦resolver⁩ و بازیابی از افت بسته را اضافه می‌کند. هیچ‌وقت پورت ۴۴۳ را نمی‌گیرد، پس در کنار پنل کار می‌کند.

هر دو روی پورت ۵۳ گوش می‌دهند، پس هر بار فقط یکی اجرا می‌شود. آن را در پنل از بخش **⁦Settings⁩** روشن کنید. به یک زیردامنه نیاز دارد که با یک رکورد ⁦NS⁩ به نود شما واگذار شده باشد. اگر دامنه‌تان روی ⁦Cloudflare⁩ است، پنل می‌تواند این واگذاری را خودکار بسازد؛ در غیر این صورت راهنمای داخل پنل رکوردهای دستی ⁦A⁩ و ⁦NS⁩ را قدم‌به‌قدم نشانتان می‌دهد. این یک پروتکل جداست: کاربران با کلاینت خودِ موتور وصل می‌شوند (⁦MasterDnsVPN⁩ یا ⁦CottenDns⁩)، نه با اپلیکیشن ⁦Nova⁩. پنل کانفیگ کلاینت را می‌سازد و خروجی می‌گیرد.

---

## گام ۱۲ - امنیت (پنل مخفی، نگهبان ورود، وب‌هوک‌ها و ۲⁦FA⁩)

⁦Nova Server⁩ چند لایه امنیتی دارد:

- **پنل پشت مسیر مخفی:** در نصب تازه، پنل پشت یک زیرمسیر مخفی تصادفی مثل ⁦https://server/p-a1b2c3/⁩ قرار می‌گیرد و هر مسیر دیگری فقط یک ۴۰۴ ساده برمی‌گرداند، تا اسکنرهایی که ⁦IP⁩ یا دامنه شما را می‌کاوند چیزی نبینند (همان ایده ⁦web base path⁩ در ⁦3x-ui⁩ و ⁦Marzban⁩). هر وقت خواستید در **⁦Settings⁩ > عمومی > دسترسی پنل** مسیر را عوض کنید، پاکش کنید تا پنل به ریشه برگردد، یا یک پورت ⁦HTTPS⁩ جداگانه بدهید.
- **نگهبان ورود (⁦Login guard⁩):** سپری به سبک ⁦fail2ban⁩ برای ورود به پنل. پس از تلاش‌های ناموفق زیاد از یک ⁦IP⁩ در یک بازه، آن ⁦IP⁩ برای مدتی مسدود می‌شود تا حدس‌زدن رمز به جایی نرسد. به‌صورت پیش‌فرض روشن است؛ تعداد تلاش، بازه و مدت مسدودی قابل تنظیم است و همان صفحه (در **⁦Settings⁩ > امنیت**) هر مسدودی فعال را با **رفع مسدودی** یک‌کلیکی نشان می‌دهد.
- **وب‌هوک‌ها (⁦Webhooks⁩):** یک ⁦POST⁩ کوچک ⁦JSON⁩ امضاشده به آدرس خودتان می‌فرستد وقتی روی نود اتفاقی می‌افتد: کاربری ساخته، ویرایش یا حذف می‌شود، منقضی می‌شود یا به سقف مصرف می‌رسد؛ نودی ثبت می‌شود؛ یا یک ⁦IP⁩ مسدود می‌شود. با تنظیم یک راز، ⁦Nova⁩ بدنه را با ⁦HMAC-SHA256⁩ امضا می‌کند و در هدر ⁦X-Nova-Signature⁩ می‌فرستد تا گیرنده مطمئن شود فراخوان واقعا از نود شما آمده. دکمه **⁦Test⁩** یک نمونه ارسال می‌کند.
- **چند مدیر و نماینده:** نقش‌های مالک، مدیر و نماینده (⁦reseller⁩) با دسترسی‌های اختصاصی. نماینده‌ها فقط کاربران خودشان را می‌بینند و روی ورودی‌های شما می‌سازند.
- **احراز هویت دومرحله‌ای (۲⁦FA⁩):** با ⁦Google Authenticator⁩ یک کد ۶ رقمی هنگام ورود اضافه کنید، برای هر مدیر جداگانه.

---

## گام ۱۳ - ربات تلگرام و ربات نصب‌کننده

⁦Nova Server⁩ یک **ربات کامل تلگرام با ⁦Mini App⁩** دارد که کل پنل را داخل تلگرام باز می‌کند. می‌توانید همه‌چیز را از تلگرام مدیریت کنید و هشدارها را همان‌جا بگیرید (⁦Mini App⁩ به یک دامنه معتبر نیاز دارد).

**ربات نصب‌کننده تلگرام:** اگر رایانه ندارید یا نمی‌خواهید درگیر ترمینال شوید، یک ربات راهنما می‌تواند شما را در ساخت ⁦VPS⁩ همراهی کند، بگوید نودتان کِی آنلاین شد، یا کل نصب را از طریق ⁦SSH⁩ برایتان انجام دهد:

⁦https://t.me/NovaServerInstaller_Bot⁩

یک ⁦REST API⁩ با احراز توکن هم موجود است (⁦/api/v1⁩) که پنل، ربات و ⁦API⁩ همگی از همان توابع سرویس داخلی استفاده می‌کنند.

---

## گام ۱۴ - به‌روزرسانی

پنل نسخه‌های جدید را بررسی می‌کند و با یک کلیک به‌روز می‌شود (⁦Settings⁩ > عمومی > به‌روزرسانی)، یا **به‌روزرسانی خودکار** را روشن کنید. اجرای دوباره دستور نصب هم یک نود موجود را به‌روز می‌کند. کاربران، ورودی‌ها و تنظیمات حفظ می‌شوند.

---

## نصب با ⁦Docker⁩ (جایگزین)

کانتینر را ترجیح می‌دهید؟ ⁦Nova⁩ یک گزینه ⁦Docker⁩ هم دارد. تکرارپذیر است، توقف و جابه‌جایی و ارتقایش آسان است و داده‌های شما روی والیوم‌های نام‌دار می‌ماند و پس از بازسازی هم باقی می‌ماند. این دستور را روی یک ⁦VPS⁩ واقعی لینوکس با کاربر ⁦root⁩ اجرا کنید:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/IRNova/Nova-Server/main/docker/nova-docker.sh)
```

همان گزینه‌های نصب‌کننده بومی را با متغیرهای محیطی می‌دهید: ⁦NOVA_ADMIN_PASS⁩، ⁦NOVA_DOMAIN⁩، ⁦NOVA_PANEL_PATH⁩ و ⁦NOVA_PANEL_PORT⁩. بعد از بالا آمدن، آدرس پنل را با این دستور پیدا کنید:

```bash
cd /opt/nova-docker && docker compose logs -f nova-node
```

**مهم:** این روش به یک هاست واقعی لینوکس با شبکه هاست نیاز دارد. روی ⁦Docker Desktop⁩ مک یا ویندوز کار **نمی‌کند**، چون آن‌ها نمی‌توانند پورت ۴۴۳ هاست را آن‌طور که یک نود لازم دارد بگیرند.

---

## حذف کامل

برای حذف کامل ⁦Nova⁩، ⁦xray⁩، ⁦sing-box⁩ و همه داده‌های ⁦Nova⁩ از سرور:

```bash
nova-uninstall
```

اول از شما تایید می‌گیرد. برای رد شدن از تایید، ⁦nova-uninstall --yes⁩ را بزنید. اگر این دستور در دسترس نبود، مستقیم اجرایش کنید:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/IRNova/Nova-Server/main/nova-uninstall.sh)
```

---

## سوالات متداول (FAQ)

<details>
<summary><b>نوا سرور چیه و با نوا پروکسی چه فرقی داره؟</b></summary>

⁦Nova Server⁩ یک ⁦VPS⁩ ساده لینوکسی را به یک نود پروکسی خصوصی و مقاوم در برابر فیلترینگ همراه با یک پنل مدیریت کامل تبدیل می‌کند. جایی که ⁦Nova Proxy⁩ روی لایه رایگان ⁦Cloudflare⁩ اجرا می‌شود و نیاز به ⁦VPS⁩ ندارد، ⁦Nova Server⁩ نسخه خودمیزبان و قدرتمندتر است: یک هسته پروکسی واقعی (⁦Xray-core⁩، ⁦sing-box⁩، ⁦AmneziaWG⁩) با هر آنچه یک اپراتور جدی نیاز دارد. اگر فقط یک پروکسی شخصی و رایگان می‌خواهید، ⁦Nova Proxy⁩ کافی است؛ اگر چند کاربر، چند نود و کنترل کامل می‌خواهید، ⁦Nova Server⁩ برای شماست.
لینک گیت‌هاب: ⁦https://github.com/IRNova/Nova-Server⁩

</details>

<details>
<summary><b>برای شروع به چی نیاز دارم؟</b></summary>

یک ⁦VPS⁩ با ⁦Ubuntu 20.04⁩ به بالا یا ⁦Debian 11⁩ به بالا (⁦x86_64⁩ یا ⁦arm64⁩) و دسترسی ⁦root⁩. یک سرور ساده حدود ۴ تا ۶ دلار در ماه کافی است. دامنه اختیاری است و فقط برای گواهی معتبر و ⁦Mini App⁩ تلگرام لازم می‌شود.

</details>

<details>
<summary><b>چطوری نصبش کنم؟</b></summary>

روی یک سرور تازه، فقط این دستور را اجرا کنید:

⁦bash <(curl -fsSL https://raw.githubusercontent.com/IRNova/Nova-Server/main/nova-node.sh)⁩

چند سوال کوتاه می‌پرسد (دامنه، مسیر مخفی پنل، پورت اضافی) و زدن ⁦Enter⁩ برای پیش‌فرض‌ها کافی است. بعد آدرس پنل را چاپ می‌کند؛ حتما ذخیره‌اش کنید.

</details>

<details>
<summary><b>رایانه ندارم، فقط گوشی دارم. می‌شه راه‌اندازی کرد؟</b></summary>

بله. سه راه دارید:
۱. هنگام ساخت ⁦VPS⁩، همان دستور نصب را در کادر ⁦User data⁩ یا ⁦Cloud-init⁩ ارائه‌دهنده بچسبانید؛ سرور خودش هنگام اولین بوت نصب می‌کند (۳ تا ۵ دقیقه) و بعد ⁦https://YOUR_SERVER_IP⁩ را در مرورگر گوشی باز می‌کنید.
۲. از ربات نصب‌کننده تلگرام استفاده کنید: ⁦https://t.me/NovaServerInstaller_Bot⁩
۳. بیشتر ارائه‌دهنده‌ها یک دکمه ⁦Console⁩ در مرورگر دارند که می‌توانید دستور را همان‌جا بچسبانید.

</details>

<details>
<summary><b>حتما باید دامنه داشته باشم؟</b></summary>

خیر. برای شروع به دامنه نیاز ندارید. بدون دامنه، نود روی ⁦IP⁩ خودش با یک گواهی خودامضا کار می‌کند و اپ ⁦Nova⁩ باز هم وصل می‌شود. دامنه فقط برای گرفتن گواهی معتبر و استفاده از ⁦Mini App⁩ تلگرام لازم است و هر وقت خواستید می‌توانید بعدا اضافه‌اش کنید.

</details>

<details>
<summary><b>چه پروتکل‌هایی پشتیبانی می‌شه؟</b></summary>

⁦VLESS⁩، ⁦VMess⁩، ⁦Trojan⁩، ⁦Shadowsocks-2022⁩، ⁦VLESS-Reality⁩ (با ⁦XTLS-Vision⁩)، ⁦Hysteria2⁩، ⁦TUIC v5⁩، ⁦NaiveProxy⁩، ⁦WireGuard⁩ بومی و ⁦AmneziaWG⁩. ترنسپورت‌ها هم شامل ⁦TCP⁩، ⁦WebSocket⁩، ⁦gRPC⁩، ⁦XHTTP⁩، ⁦HTTPUpgrade⁩ و ⁦mKCP⁩ روی ⁦TLS⁩ یا ⁦Reality⁩ هستند. همه روی یک صفحه ⁦Inbounds⁩ مدیریت می‌شوند.

</details>

<details>
<summary><b>چطور یک کاربر اضافه کنم و وصل بشم؟</b></summary>

در بخش ⁦Users⁩ یک کاربر بسازید و لینک اشتراکش را کپی کنید. این لینک همه تنظیمات اتصال را در خود دارد و خودش به‌روز می‌ماند. آن را در اپ ⁦Nova⁩ باز کنید، کد ⁦QR⁩ را اسکن کنید، یا در یک کلاینت معمولی مثل ⁦v2rayNG⁩، ⁦sing-box⁩ یا ⁦Clash⁩ بچسبانید.

</details>

<details>
<summary><b>رمز پنل یا مسیر مخفی رو فراموش کردم، چیکار کنم؟</b></summary>

روی سرور این دستور را اجرا کنید:

⁦nova-passwd 'YourNewPassword' --clear-2fa⁩

این دستور رمز را بازنشانی می‌کند و آدرس فعلی پنل (همراه با مسیر مخفی) را هم چاپ می‌کند. گزینه ⁦--clear-2fa⁩ هم ۲⁦FA⁩ را پاک می‌کند اگر به ⁦authenticator⁩ دسترسی ندارید.

</details>

<details>
<summary><b>مسیر مخفی پنل یعنی چی و چرا مهمه؟</b></summary>

در نصب تازه، پنل پشت یک زیرمسیر تصادفی مثل ⁦https://server/p-a1b2c3/⁩ قرار می‌گیرد و هر مسیر دیگری فقط یک ۴۰۴ ساده برمی‌گرداند. این کار باعث می‌شود اسکنرهایی که ⁦IP⁩ یا دامنه شما را می‌کاوند چیزی نبینند (همان ایده ⁦web base path⁩ در ⁦3x-ui⁩ و ⁦Marzban⁩). می‌توانید در ⁦Settings⁩ > عمومی > دسترسی پنل مسیر را عوض کنید یا پاکش کنید تا به ریشه برگردد.

</details>

<details>
<summary><b>چطور چند سرور رو از یک پنل مدیریت کنم؟</b></summary>

پنل را یک بار روی سرور اصلی نصب کنید. بعد در صفحه ⁦Nodes⁩ روی «افزودن نود با یک دستور» بزنید و تک‌خطی را روی یک ⁦VPS⁩ تازه اجرا کنید. سرور جدید در حالت نود مدیریت‌شده نصب می‌شود و همه‌چیز از پنل اصلی کنترل می‌شود. توکن اتصال یک‌بارمصرف است و بعد از ۲۴ ساعت منقضی می‌شود. کاربران و پروتکل‌ها مرکزی‌اند و با گزینه «⁦Run on⁩» روی نودها قرار می‌گیرند.

</details>

<details>
<summary><b>پل ایران چیه و چطور راه می‌افته؟</b></summary>

پل ایران یعنی یک سرور با ⁦IP⁩ تمیز داخل ایران جلوی خروج خارجی‌تان قرار می‌گیرد تا کاربران به آن ⁦IP⁩ تمیز وصل شوند و ترافیک به خروج خارجی تونل شود. لازم نیست کل استک را روی سرور ایران نصب کنید: در پنل نود خارجی بخش ⁦Tunnels⁩ را باز کنید، تونل را تنظیم کنید، «ساخت دستور پل ایران» را بزنید و آن یک دستور را روی ⁦VPS⁩ ایران اجرا کنید. فقط بک‌اند تونل (⁦Backhaul⁩، ⁦BackPack⁩، ⁦rathole⁩ یا ⁦wstunnel⁩) نصب می‌شود. تونل هم ⁦TCP⁩ و هم ⁦UDP⁩ را منتقل می‌کند تا ⁦Hysteria2⁩ کار کند.

</details>

<details>
<summary><b>تونل DNS چه زمانی به درد می‌خوره؟</b></summary>

تونل ⁦DNS⁩ ترافیک را داخل کوئری‌های ⁦DNS⁩ جابه‌جا می‌کند و آخرین راه انتقال است: وقتی همه‌چیز مسدود است ولی ⁦DNS⁩ هنوز جواب می‌دهد باز هم کار می‌کند. دو موتور دارد: ⁦MasterDnsVPN⁩ (پیش‌فرض ساده) و ⁦CottenDns⁩ (تواناتر، با ⁦resolver⁩های ⁦DoT/DoH⁩ برای شبکه‌های بسیار خصمانه). به یک زیردامنه با رکورد ⁦NS⁩ واگذارشده نیاز دارد و کاربران با کلاینت خودِ موتور وصل می‌شوند، نه با اپ ⁦Nova⁩.

</details>

<details>
<summary><b>نگهبان ورود (Login guard) چیه؟</b></summary>

یک سپر به سبک ⁦fail2ban⁩ برای ورود به پنل. اگر از یک ⁦IP⁩ تلاش‌های ناموفق زیادی برای ورود انجام شود، آن ⁦IP⁩ برای مدتی مسدود می‌شود تا حدس‌زدن رمز به جایی نرسد. به‌صورت پیش‌فرض روشن است و در ⁦Settings⁩ > امنیت می‌توانید تعداد تلاش، بازه و مدت مسدودی را تنظیم کنید و مسدودی‌های فعال را یک‌کلیکی رفع کنید.

</details>

<details>
<summary><b>ربات تلگرام چه کارهایی می‌کنه؟</b></summary>

⁦Nova Server⁩ یک ربات کامل تلگرام با ⁦Mini App⁩ دارد که کل پنل را داخل تلگرام باز می‌کند؛ می‌توانید کاربران و تنظیمات را از تلگرام مدیریت کنید و هشدارها را همان‌جا بگیرید. ⁦Mini App⁩ به یک دامنه معتبر نیاز دارد. یک ربات نصب‌کننده جدا هم هست که در ساخت و نصب نود کمکتان می‌کند: ⁦https://t.me/NovaServerInstaller_Bot⁩

</details>

<details>
<summary><b>چطور SSL و گواهی معتبر بگیرم؟</b></summary>

⁦Nova Server⁩ سه راه دارد و هر سه خودکار تمدید می‌شوند: ⁦Let's Encrypt⁩ یک‌کلیکی، ⁦Cloudflare⁩ کاملا خودکار (⁦DNS⁩ خودکار به‌همراه وایلدکارت) یا چسباندن گواهی ⁦Origin⁩ خودتان. اگر دامنه دارید، پنل هنگام نصب یا از بخش تنظیمات خودش گواهی رایگان می‌گیرد و نیازی به درگیری دستی با پورت ۸۰ نیست.

</details>

<details>
<summary><b>چطور به‌روزش کنم؟ کاربرام حفظ می‌شن؟</b></summary>

پنل نسخه‌های جدید را بررسی می‌کند و با یک کلیک به‌روز می‌شود (⁦Settings⁩ > عمومی)، یا «به‌روزرسانی خودکار» را روشن کنید. اجرای دوباره دستور نصب هم یک نود موجود را به‌روز می‌کند. در همه حالت‌ها کاربران، ورودی‌ها و تنظیمات حفظ می‌شوند.

</details>

<details>
<summary><b>می‌تونم با Docker نصب کنم؟</b></summary>

بله، به‌عنوان جایگزین نصب‌کننده تک‌خطی. تکرارپذیر است و داده‌ها روی والیوم‌های نام‌دار می‌ماند. این را روی یک ⁦VPS⁩ واقعی لینوکس با ⁦root⁩ اجرا کنید:

⁦bash <(curl -fsSL https://raw.githubusercontent.com/IRNova/Nova-Server/main/docker/nova-docker.sh)⁩

توجه: روی ⁦Docker Desktop⁩ مک یا ویندوز کار نمی‌کند، چون نمی‌توانند پورت ۴۴۳ هاست را بگیرند. به شبکه هاست نیاز دارد.

</details>

<details>
<summary><b>چطور مطمئن بشم فایل نصب دستکاری نشده؟</b></summary>

اسکریپت‌های نصب در مخزن، شل ساده‌ای هستند که می‌توانید قبل از اجرا بخوانیدشان. ایجنت نود به‌صورت ⁦nova-node-agent.tar.gz⁩ همراه با یک ⁦SHA256SUMS⁩ منتشر می‌شود و نصب‌کننده به‌صورت خودکار ایجنت را با آن چک‌سام تطبیق می‌دهد و در صورت عدم تطابق متوقف می‌شود. برای بررسی دستی:

⁦curl -fsSLO https://raw.githubusercontent.com/IRNova/Nova-Server/main/nova-node-agent.tar.gz⁩
⁦curl -fsSL https://raw.githubusercontent.com/IRNova/Nova-Server/main/SHA256SUMS | sha256sum -c⁩

</details>

<details>
<summary><b>چطور نوا سرور رو کامل حذف کنم؟</b></summary>

روی سرور این دستور را اجرا کنید:

⁦nova-uninstall⁩

اول از شما تایید می‌گیرد؛ برای رد شدن از تایید ⁦nova-uninstall --yes⁩ را بزنید. این کار ⁦Nova⁩، ⁦xray⁩، ⁦sing-box⁩ و همه داده‌ها را پاک می‌کند. اگر دستور در دسترس نبود، مستقیم اجرایش کنید:

⁦bash <(curl -fsSL https://raw.githubusercontent.com/IRNova/Nova-Server/main/nova-uninstall.sh)⁩

</details>

<details>
<summary><b>مشکل دارم، چطور کمک بگیرم؟</b></summary>

اول راهنمای «راه‌اندازی گام‌به‌گام» داخل خود پنل را ببینید (به هر سه زبان موجود است). اگر حل نشد، در گروه تلگرام بپرسید: ⁦https://t.me/irnovaproxy_group⁩ و کانال رسمی: ⁦https://t.me/irnova_proxy⁩

</details>
