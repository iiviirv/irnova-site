# Cloudflare Pages config for the "nova-website" project.
# Production deploys automatically from the connected Git repo (branch: main),
# which is the normal path: push to main and Pages builds it.
#
# The name below must match the Pages project exactly. It read "novaproxy" until
# 2026-08-07, so `npm run deploy` failed with "project does not exist"; the Git
# integration masked it because nobody needed the manual path.
name = "nova-website"
pages_build_output_dir = "./dist"
compatibility_date = "2026-07-30"

# Custom domains served by this Pages project (production). The domain-to-project
# binding is verifiable in the Cloudflare dashboard for the "novaproxy" project
# named above; declared here so the deployed hostnames are visible in source:
#   - novaproxy.online            (site + credential-free /setup instructions)
#   - www.novaproxy.online

# Visit & install counters used by functions/api/stats.js. Same DB for
# production and preview — preview traffic is negligible and it keeps setup
# simple. Created on the account that owns this Pages project (nova-website).
[[d1_databases]]
binding = "nova_stats"
database_name = "nova-stats"
database_id = "83842750-f0d3-45c1-ba64-08902db29ee2"

[[env.preview.d1_databases]]
binding = "nova_stats"
database_name = "nova-stats"
database_id = "83842750-f0d3-45c1-ba64-08902db29ee2"
